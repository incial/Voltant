/**
 * Feature Components
 * 
 * Interactive feature components that provide specific functionality
 * across the application (e.g., chat, notifications, modals).
 */

export { default as ChatButton } from './ChatButton';
