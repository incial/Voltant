/**
 * Layout Components
 * 
 * Core layout components that structure the application shell.
 * These components are used across all pages for consistent layout.
 */

export { default as Navbar } from './Navbar';
export { default as Footer } from './Footer';
